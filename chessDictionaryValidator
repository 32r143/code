rows = ['1','2','3','4','5','6','7','8']
columns = ['a','b','c','d','e','f','g','h']
cells= []
for single in rows:
    for each in columns:
        cell =single+ each
        cells.append(cell)
#        print(cell,end = ',')
print(cells)
